const isValidId = require("./isValidId");
const validateBody = require("./validateBody");
const isUniquePhoneNumber = require("./isUniquePhoneNumber");

module.exports = { validateBody, isValidId, isUniquePhoneNumber };
